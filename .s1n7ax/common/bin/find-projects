#!/bin/env bash

quick_exit ()
{
  read VAR
  if [ ! -z "${VAR}" ]; then
    echo $VAR
    PID=$$
    kill $PID 2&> /dev/null
  fi
}

fd -I -i -H \
  -t d ^.git$ ~/.config ~/Workspace \
  -x 'echo' '{//}' | sk | quick_exit
